title_prefix = <%= @titleprefix %>
enable_icinga_features = 1
<Component Thruk::Backend>
<% @servers.each do |server| %>
    <peer>
        name    = icinga
        id      = 
        type    = livestatus
        <options>
            peer          = <%= server %>:6557
            remote_name   = icinga
        </options>
    </peer>
    <peer>
<% end -%>
</Component>
