title_prefix = <%= @titleprefix %>
enable_icinga_features = 1
<Component Thruk::Backend>
<% @servers.each do |server| %>
    <peer> 
        type    = livestatus
        name    = <%= server %>
        <options>
            peer          = <%= server %>:6557
        </options>
    </peer>
<% end -%>
</Component>
